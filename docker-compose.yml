version: '3.8'

services:
  fastapi-adk:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vnstock-agent-api
    ports:
      - "8002:8002"
    environment:
      # Google AI API key (BẮT BUỘC)
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      # MCP Server config (optional, có default trong configs/mcp_config.yaml)
      - MCP_SERVER_URL=${MCP_SERVER_URL:-https://mcp-server-vietnam-stock-trading.onrender.com}
      - MCP_TIMEOUT=${MCP_TIMEOUT:-30}
      # Port nội bộ cho Uvicorn (run_server.py đọc từ biến PORT)
      - PORT=8002
      # FastAPI settings (JSON array format cho list[str])
      # Nếu không set, sẽ dùng default ["*"] từ config.py
      # Để override, set trong .env: BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:8080"]
    env_file:
      - .env
    volumes:
      # Mount configs để có thể chỉnh sửa mà không rebuild
      - ./configs:/app/configs:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import httpx; httpx.get('http://localhost:8002/health', timeout=5)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

